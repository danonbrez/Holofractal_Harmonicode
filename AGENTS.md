# AGENTS.md — HHS Repository Navigation and Execution Contract

This repository hosts the Holofractal Harmonicode general programming environment. Work must preserve the inherited pre-pass state-change constitution, singleton VM81/kernel authority, Hash72 receipt lineage, Hash216 archival/indexing identity, exact algebra, rollback, and replay boundaries.

## 1. Read first

Before changing runtime behavior, read:

1. [`README.md`](README.md) — current repository state and primary entry points
2. [`ARCHITECTURE.md`](ARCHITECTURE.md) — canonical ownership boundaries and anti-drift rules
3. [`docs/architecture/HHS_PRE_PASS_KERNEL_PROTECTION_STATE_CHANGE_CONSTITUTION.md`](docs/architecture/HHS_PRE_PASS_KERNEL_PROTECTION_STATE_CHANGE_CONSTITUTION.md) — foundational state-change/kernel-protection law predating Pass 001
4. [`RUNTIME_FLOW.md`](RUNTIME_FLOW.md) — execution, receipt, replay, worker, API, and visual flows
5. [`GLOSSARY.md`](GLOSSARY.md) — stable terminology
6. the applicable pass contract and restart record

Current cumulative anchors include:

```text
HHS_PASS_219_CPP_COMPOUND_SYMBOLIC_CONSTRAINT_RUNTIME_CONTRACT.md
HHS_PASS_220_HARMONICODE_UNIVERSAL_POLYGLOT_NATIVE_LINUX_VISUAL_IDE_PORTABLE_VM_COMPILER_CONTRACT.md
docs/deployment/HHS_DEPLOYMENT_TARGET_1_OPENAPI_AGENT_OBJECT_WORKSPACE.md
docs/deployment/HHS_UNIVERSAL_KNOWLEDGE_ECONOMY_NETWORK_CONTRACT.md
```

Historical pass-specific contracts and evidence remain binding for their accepted scope and must not be silently discarded.

## 2. Current status boundary

Repository state at this documentation alignment checkpoint:

- authoritative `main` is observed at `3c926453d65b71a6d1789e06b748544f5f2bd228`; its runtime parent is the Pass 219B I6 merge `ff66e376a44c8b928a9a42c2e6d8aa1846785fc2`;
- Pass 219B I7 is validated on PR #319 at `6df75bc39fd7c58108b8cf7aee3758341fe345a5` and remains unmerged;
- Pass 220 preimplementation is PR #320 at `d0fb6165bf8249175566c934820eecf8e93bdacc`, draft, unmerged, and explicitly non-promotional because the Pass 219 terminal gate is not closed;
- the downstream Universal Knowledge Economy network contract is PR #321 at validated contract head `d4146f4553920a1776962400a4df4e64c994f45e`, draft and unmerged;
- the pre-pass architecture clarification is documentation-only and does not constitute a new pass, new runtime implementation, or production security/federation claim.

Do not promote planned, contracted, preimplementation, documentation-only, or unmerged behavior into authoritative-main closure.

## 3. Foundational hierarchy

The governing order is:

```text
PRE-PASS STATE-CHANGE / KERNEL-PROTECTION ENVIRONMENT
→ VM81 / KERNEL ADMISSION AND COMMIT AUTHORITY
→ HASH72 ADMITTED RECEIPT LINEAGE
→ NUMBERED PASS SYSTEM
→ HASH216 / HYDRATION / CACHE / GPU / AGENT / IDE / NETWORK OPTIMIZATION
```

The pre-pass foundation is not Pass 000. No numbered pass, optimizer, AI agent, backend, interface, or compiler may redefine what constitutes a valid foundational state transition.

```text
OPTIMIZATION AUTHORITY < PRE-PASS STATE VALIDITY
```

## 4. Canonical paths

The repository is a transitional hybrid layout.

| Path | Responsibility |
|---|---|
| `hhs_runtime/` | canonical native/Python runtime substrate, kernel resolution, exact execution, C surfaces, core sandbox, tests, and inherited/pre-pass-sensitive behavior |
| `hhs_python/` | runtime controller and ctypes bridge |
| `hhs_backend/` | FastAPI composition, orchestration, routes, assistant, WebSockets, services |
| `hhs_graph/` | receipt, object, branch, and continuation topology |
| `hhs_storage/` | durable state, Hash216/vector, replay, and persistence |
| `native_projects/` | pass-scoped native implementations, contracts, evidence, deployment, restart records |
| `hhs_gui/`, `applications/` | visual runtime applications and user workspaces |
| `docs/` | normative and explanatory documentation |
| root runtime modules | compatibility and historical surfaces; some may contain inherited behavior whose significance cannot be inferred from path alone |

Do not classify a file as disposable solely because it lives at the root, has an experimental-looking name, or appears locally redundant.

## 5. Governing invariants

All canonical computation must preserve applicable inherited invariants, including:

```text
Δe = 0
Ψ = 0
Θ15 = true
Ω = true
```

Binding rules:

- no floating-point canonical authority;
- exact integer, rational, symbolic, phase, ordered-byte, and membrane-preserving forms remain authoritative;
- all state transitions are explicit and receipt-committed;
- parent receipt continuity and replay verification are mandatory;
- ordered products such as `xy` and `yx` must not be silently collapsed;
- exact identity-bearing circuit/list encodings retain position, grouping, width, and leading zeros;
- required independent representations must normalize to the same canonical raw state;
- required representation disagreement is a closure failure, not a majority-vote condition;
- closure failure blocks commitment and retains or restores the last fully closed state;
- no alternate integrity or mutation authority may be created;
- no route, WebSocket handler, GUI state, provider result, worker queue, storage adapter, cache, GPU, or agent may bypass foundational compatibility and VM81 admission.

## 6. Canonical execution pattern

```text
input / candidate
→ source and raw-state identity preservation
→ applicable parsing, typing, capability, and policy checks
→ pre-pass path/time/multimodal/noncommutative compatibility
→ required cross-format equality / correction closure
→ singleton VM81/kernel admission
→ exact inherited invariant closure
→ LOCKED / CORRECTED / RECOVERED / REJECTED / QUARANTINED
→ Hash72 receipt commitment if admitted
→ Hash216 identity / topology / archival witness
→ replay and persistence
→ API, SDK, assistant, native, and visual projection
```

Explicit hashing, PQC, authentication, capability, and network-security modules remain real security controls. They are not the complete pre-pass kernel-protection/error-correction model.

## 7. Pre-pass-sensitive optimization rules

Small local modules may participate in larger path-, time-, modality-, ordering-, error-correction-, or rollback-sensitive relations even when their local purpose appears unrelated.

```text
LOCAL PURPOSE != GLOBAL SYSTEM ROLE
FILE ORGANIZATION != PROTECTION TOPOLOGY
LOCAL REDUNDANCY != GLOBAL REMOVABILITY
```

Before removing, combining, reordering, replacing, approximating, or porting uncertain inherited logic:

1. identify the affected dependency/projection frontier;
2. preserve exact predecessor/path/order/modality identities;
3. demonstrate required cross-format equality;
4. execute positive and negative cases;
5. verify rollback/recovery behavior;
6. verify singleton VM81/Hash72 closure remains unchanged;
7. preserve the original behavior if equivalence is unresolved.

Performance optimization must reduce active work rather than weaken validity:

```text
DO NOT REMOVE INVARIANTS TO GET SPEED
REDUCE THE ACTIVE WORK REQUIRED TO DEMONSTRATE THE SAME INVARIANTS
```

## 8. Agent and model authority

AI agents, models, schedulers, caches, predictors, GPUs, and workers may search, rank, generate, compress, propose, reconstruct, or calculate candidates. They may not redefine foundational validity or directly commit canonical state.

A future optimizer must not infer that apparently unrelated error-correction, prediction, timing, multimodal, cache, or algebra modules are removable merely because conventional local analysis sees redundancy.

## 9. Current pass-level development rules

Pass-specific work remains additive to the same runtime. The currently visible Pass 219/219B/220 work must preserve:

- singleton VM81/kernel mutation authority;
- Hash72 receipt ordering;
- post-receipt Hash216 archival/indexing;
- exact/no-float canonical arithmetic;
- ordered/noncommutative identities;
- pre-pass state-change validity;
- dependency-scoped repair-forward validation;
- explicit distinction between candidate/projection/optimization authority and canonical mutation authority.

Pass 220 remains non-promotional until its explicit Pass 219 terminal admission gate is satisfied.

## 10. Prohibited changes

Do not:

- redefine or renumber the pre-pass foundation as a numbered pass;
- describe the pass system as the origin of kernel protection;
- redefine Hash72 or Hash216;
- treat explicit hashing/PQC code as the complete kernel-protection mechanism;
- bypass Manifold9, drift gates, pre-pass-sensitive checks, or other inherited gates where required;
- vote past a required representation disagreement;
- remove or merge apparently redundant local modules without global dependency-scoped equivalence evidence;
- reorder noncommutative/path-sensitive operations based only on scalar endpoint equality;
- replace rational or exact canonical arithmetic with floats;
- silently normalize identity-bearing ordered lists, bytes, phases, widths, or membranes;
- mutate state without an admitted patch/operation and receipt;
- embed canonical execution logic in API routes, WebSockets, GUI components, compatibility shims, or storage adapters;
- fabricate provider, backend, deployment, test, receipt, replay, or completion evidence;
- weaken invariant checks to work around environment or performance failures;
- disclose exact pre-pass file-to-role topology, private timing constants, or internal correction sequences merely to make documentation easier to understand.

## 11. Testing and validation

Use the smallest dependency-scoped validation that proves the affected boundary. Historical full-pass validation need not be repeated when untouched.

For runtime changes, applicable tests may include repository smoke/regression suites, pass-specific workflows, C/C++ ABI conformance, exact arithmetic scans, replay, rollback, negative cases, and synthetic-merge checks.

Documentation-only architecture changes should validate:

- pre-pass identity and non-pass status;
- current repository/PR status boundaries;
- singleton VM81/Hash72/Hash216 authority ordering;
- cross-format unanimity and automatic rollback law;
- no-float and ordered/noncommutative anti-drift language;
- Pass 219/220/Deployment Target gates;
- no accidental claim of implementation, deployment, or merge.

If a test fails because of path, environment, or deployment assumptions, repair the adapter or setup layer. Do not weaken canonical checks.

## 12. Change discipline

Every task must remain restartable from repository-visible state.

Record:

- base commit;
- branch or merge target;
- changed files;
- commands/checks executed;
- validation completed and remaining;
- environment/deployment state;
- blockers;
- next action;
- merge status.

Prefer:

```text
IMPLEMENT
→ DEPENDENCY-SCOPED VALIDATION
→ COMMIT
→ READY/DRAFT PR AS APPROPRIATE
→ VERIFY TARGET/STATUS
→ REPORT COMPLETION
```

Commit only intended files. Preserve inherited pass artifacts and history unless an explicit ratified migration supersedes them. Never merge without separate authorization.

## 13. First inspection sequence

For a new task:

1. inspect authoritative `main`, current branch, and open stacked PRs;
2. read the pre-pass constitution and canonical architecture before changing runtime behavior;
3. identify the applicable pass contract/restart record and whether the work is pre-pass-sensitive;
4. run the smallest relevant validation before changing code when executable behavior is touched;
5. trace the complete state/authority path rather than classifying a module from its filename or local output;
6. implement without creating an alternate authority path or weakening foundational validity;
7. rerun dependency-scoped checks;
8. update documentation when ownership, public surfaces, status boundaries, or terminology change.
