[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hhs-pass133-checkpoint"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = ["sympy>=1.14"]

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["."]
