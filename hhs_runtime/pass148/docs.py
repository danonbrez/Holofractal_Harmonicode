from __future__ import annotations

PASS148_PUBLIC_DOCUMENTS = [
    {
        "name": "PASS_148_NATIVE_SEMANTIC_AUTHORITY_MEMBRANE.md",
        "text": """# HHS Pass 148 Native Semantic Authority Membrane\n\nRepresentation may vary while native semantic authority must not drift. Every proposition is classified as DECLARED_SYSTEM_LAW, DERIVABLE_CONSEQUENCE, CONTROL_PROJECTION, UNRESOLVED_EXPRESSION, or NARRATIVE_EXTRAPOLATION. External model output has zero native commit authority. Control projections remain non-native. Promotion requires a witnessed authorization path. O is distinct from pi, and Delta normalization laws do not authorize silent scalarization to one.\n""",
    },
    {
        "name": "PASS_148_SEMANTIC_CLI_QUICKSTART.md",
        "text": """# Pass 148 Semantic CLI\n\nUse `hhs semantic analyze`, `analyze-document`, `derive`, `project`, `promotion-request`, `rule show`, `replay`, and `audit`. Use `hhs command describe semantic analyze`, `hhs api-contract describe /api/v1/semantic-membrane/analyze`, and `hhs schema inspect pass148-proposition` for public contracts. External agents may analyze, derive, project, request promotion, retrieve, audit, and replay but cannot evaluate promotions or synchronize the authoritative registry.\n""",
    },
    {
        "name": "PASS_148_PROJECTION_AND_NARRATIVE_ISOLATION.md",
        "text": """# Projection and Narrative Isolation\n\nCOMMUTATIVE_FIELD_CONTROL_V1, IEEE754_BINARY64_CONTROL_V1, conventional real or complex analysis, symbolic CAS output, and narrative world models are isolated namespaces. Their outputs cannot mutate native state, satisfy native gates, repair native results, or generate canonical native receipts. Fiction can contain candidate analytic propositions, but fictional events and technical appendices remain narrative-scoped until separately admitted.\n""",
    },
]
