# Pass 081 Recursive Gate Semantics
Delimiter-preserving parsing precedes gate discovery. Every `A==B` creates `g_i`, local `P_gi`, forward and reverse paths, an exact symbolic closure tensor, and a tri-state closure classification. Stable unresolved systems are canonical receipts, not rejection.
