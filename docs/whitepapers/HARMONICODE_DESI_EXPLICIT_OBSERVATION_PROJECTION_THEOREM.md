# HARMONICODE Explicit DESI Observation-Projection Theorem

## 1. Exact public observation state

Let the DESI row be:

~~~text
z = 233/100
d_H = D_H/r_d = 1079/125
d_M = D_M/r_d = 3899/100.
~~~

All three are exact rational source objects.

## 2. Relativistic observation map

The I040 map is:

~~~text
Pi_DESI:
(z, d_H, d_M)
->
(
 z,
 1+z,
 d_H,
 d_M,
 1/d_H,
 d_M/d_H,
 z*d_M^2*d_H
).
~~~

The output fields are typed as:

~~~text
redshift target
one-plus-redshift target
normalized Hubble distance
normalized transverse comoving distance
H*r_d/c0
D_M/D_H
(D_V/r_d)^3.
~~~

## 3. Exact solved values

The map yields:

~~~text
1+z = 333/100

H*r_d/c0 = 125/1079

D_M/D_H = 19495/4316

(D_V/r_d)^3
= 3821939746807/125000000.
~~~

These are direct algebraic consequences of the explicit observational
definitions. No parameter search is used.

## 4. Parallel HHS carrier

Every input numeral remains simultaneously represented by its:

~~~text
exact decimal/rational source
palindromic IEEE transport state
exact IEEE dyadic
exact representation residue
5184-character BigInt state
I037 equation/proof root
Lane 5 constructor state.
~~~

No one representation replaces another.

## 5. Three-copy phase binding

Let the projection root be `R_Pi`.

Then:

~~~text
G24[-].root = R_Pi
G24[0].root = R_Pi
G24[+].root = R_Pi.
~~~

Each copy contains the complete observation state and exact residues.

## 6. Data-bound closure

Define:

~~~text
U_data(S_i) =
U_I039(S_i)
AND Pi_DESI(S_i)
AND C_parallel(S_i)
AND C_5184(S_i)
AND C_phaseCopies(S_i).
~~~

I040 returns `CLOSE` exactly when all predicates close.

## 7. Corpus theorem

For rows `D_1,...,D_n`, define:

~~~text
CorpusClose
=
AND_i U_data(Pi_DESI(D_i)).
~~~

No mean, weighted mean, likelihood, posterior, or soft score substitutes for
this Boolean conjunction.

Thus one failed row produces:

~~~text
CorpusClose = false
status = REJECT.
~~~

## 8. Scope

I040 solves the explicit DESI observational definitions and binds them to the
existing HHS unification substrate. It does not create an implicit mapping into
`A,B,P,p,q,x,y,z,w`; any later native-variable identification must be declared
as its own constructor and tested against this frozen projection.
