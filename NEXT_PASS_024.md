# Next Pass 024

## Recommended Priority

Create the first **live semantic adapter** from the Pass 023 static-adapter batch.

## Candidate Focus

Start with semantic/database functionality because it is core to the product objective:

```text
verbatim semantic database
→ modality ingestion
→ database integration layer
→ guarded semantic adapter
→ service registry
→ API/GUI reachability
```

## Constraints

- No direct legacy execution path.
- All inputs through IO gateway and canonical runtime contract.
- All outputs through Hash72/u^72 witness and foundational audit.
- Preserve proposition identity and no-flatten nested carrier structures.
- Rollback or block if vector/cache/database propagation cannot be receipt-backed.
