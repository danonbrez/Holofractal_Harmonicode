from __future__ import annotations

PUBLIC_DOCUMENTS = [
    {"name": "PASS_147_EXTERNAL_AGENT_OPACITY.md", "text": """# HHS Pass 147 External-Agent Opacity\n\nPotential lawful capability is complete while privileged internal access is zero. The external actor constructs operations through documented CLI, API, schemas, scripts, sandboxes, LVMs, boundary contracts, tests, receipts, and replay. Direct kernel memory, unrestricted SQL, hidden object graphs, raw capability tokens, internal scheduler control, and undocumented native entrypoints are prohibited. Every execution is boundary-constructed and receipt-closed.\n\nCanonical equation: COMPLETE FUNCTIONAL POTENTIAL + ZERO PRIVILEGED ACCESS + PUBLIC PROCEDURAL CONSTRUCTION = HHS EXTERNAL-AGENT OPACITY.\n"""},
    {"name": "PUBLIC_SURFACE_QUICKSTART.md", "text": """# Public Surface Quickstart\n\nUse `hhs surface list`, `hhs surface graph`, `hhs command describe`, `hhs api describe`, `hhs schema inspect`, `hhs error explain`, `hhs runtime types`, and `hhs examples`. Create an external identity with `hhs agent bootstrap`. Execute work with `hhs agent execute` and the one-time credential. All target commands pass through Pass 146 minimum-path construction.\n"""},
    {"name": "PUBLIC_SECURITY_MODEL.md", "text": """# Public Security Model\n\nNo operation exists without a boundary. Public capability discovery does not grant execution authority. A public command contract declares inputs, required capabilities, mutation behavior, reversibility, failure states, and receipt behavior. Child operations may narrow but never expand parent authority. O and pi remain distinct symbols. IEEE floats are projections and never canonical exact authority.\n"""},
]
