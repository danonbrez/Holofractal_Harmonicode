# Pass 219 I121.3 — Main-Aligned Exact VM81 Candidate Adapter Restart Record

## Repository authority

- Repository: `danonbrez/Holofractal_Harmonicode`
- Canonical authority base: `main @ f5d8fdc014d888f93c0d85d40d2a2c0c198eefdf`
- Branch: `agent/pass219-orthogonal-glyph-parallel-membrane-1-21`
- PR: `#315`
- Merge target: `main`
- Merge state: unmerged; canonical `main` unchanged
- Delivery mode: append-only / repair-forward / no rebase / no squash / no force-push

The branch is directly rooted in canonical `main`. Side-branch implementation checkpoints such as `85c237023e778e655f38f6363bab7f08907fa9b2` and later 1.21 development SHAs are historical implementation evidence only and do not supersede the inherited pass-history authority of `main`.

## Canonical inherited authority anchors

The I121.3 adapter is constrained by executable/contract authority already present in the main lineage:

```text
Pass 159 merged HARMONICODE/VM81 toolchain
8e7ffb22286f5b6b377c778276c333607a7c2a03

Pass 168 VM81 64×81 = 5,184 circuit contract
eb88a4b88ab8c598458c0e48c0f4f9db77f81654

Pass 169 whole-expression algebra enforcement / VM81 exact symbolic authority
62e296024b27ff3209e3ef2ac4a2d565e03296ca

Pass 186 ordered noncommutative x86_64 VM81 ABI
fd42056c22071d290945b02efe3a5752aaa3d737

Pass 188 executable Bott/G243 transition + replay runtime
c77e3feef42448a111d8b8912a1d1cb157d51925

Pass 189 executable HQLH/kappa41 contextual hydration runtime
a1a55a4f621ff3678f5af81119439e9558cf9db4

Frozen Pass 219 I118 ancestor
e87bc42b17c03ff98f691838b8d573a5bdf46ff2
```

## Implemented I121.3 capability

Public surface:

- `hhs_exact_pass219_vm81_adapter_version`
- `hhs_exact_pass219_vm81_lower_monolithic_structure`
- `hhs_exact_pass219_vm81_execute_candidate`
- `hhs_exact_pass219_vm81_replay_candidate`

The adapter:

1. binds the frozen 348-byte native Pass 219 1.20 source identity;
2. scans 34 matched parenthesis shells and 15 literal equality half-gates;
3. closes the permanent source-defined operation fabric as `49 + 15 = 64` threads;
4. retains a typed 24-glyph-to-VM81-cell map for `P,t,p,q,∆,m,b,c,u,s,x,y,z,w,xy,yx,zw,wz,At,f,Bt,A,B,a²`;
5. lowers those source-structure and derived threads to exact inherited VM81 opcodes;
6. imports one exact `81 × 64-bit = 648-byte` candidate frame into an isolated VM81 execution state;
7. binds source SHA state into deterministic execution initialization;
8. executes the actual inherited `vm81_step` kernel rather than a host substitute evaluator;
9. exports the resulting exact VM81 frame, ordered phase surface, witness flags and receipt-chain tip;
10. independently re-executes the same program/frame and requires exact equality of frame, receipt identities, witness state, step count, coverage state and ordered phase surface.

## Proof and authority boundary

I121.3 intentionally distinguishes exact candidate execution from full symbolic proof.

Current invariant:

```text
source_structure_complete = true
effectful_lowering_complete = true
source_semantics_complete = false
full_symbolic_identity_required = true
full_symbolic_identity_gate_supported = false
canonical_monolithic_proof = false
floating_point_authority = false
vm81_mutation_authority = false
hash72_commit_authority = false
```

A caller asserting `source_semantics_complete=1` is rejected. A raw proof packet, scalar equality, projected phase collision, Pass159 foundation receipt, or cache/GPU result cannot promote this adapter to canonical proof.

Canonical closure remains downstream of a source-preserving whole-expression evaluator that resolves the complete monolithic equality chain in one candidate state and then obtains exact VM81 admission/receipt/replay authority under inherited Pass 169 semantics.

## Related repair-forward hardening on this branch

The current main-aligned tranche also repairs:

- monolithic 1.20 Hash72/Hash216 validation so embedded NUL values reject;
- undeclared stage/family/equality-edge mask bits so malformed proof packets reject rather than being masked away;
- ordered octonion public APIs so malformed derived `xy/yx/zw/wz` state fails closed before multiplication/channel access;
- membrane lane identity so full proof, receipt and verification state is Hash216-bound;
- contradiction identity so verification-decision and proof-identity divergence is visible;
- global graph identity so exact length-delimited `a²` and `∆` bytes participate, not only an equality Boolean;
- large membrane conformance fixtures so they are heap-backed rather than exceeding the default Linux process stack;
- focused workflow lineage so authority is proven from canonical-main pass ancestors, not side-branch implementation SHAs;
- root `make c-abi` dependency tracking so changes to the exact ABI aggregate, directly included `.inc` modules and public aggregate headers invalidate `libhhs_runtime.so`.

## Validation surface

Focused workflow:

`.github/workflows/pass219-exact-vm81-candidate-adapter-1-21-3.yml`

Required exact and synthetic gates include:

```text
canonical-main authority ancestry
frozen native source SHA-256
no float/double authority in I121.3
no commit/persist/admit export
cumulative exact ABI compile
actual exact VM81 candidate execution
exact replay equality
negative semantic-completion/source/cell/replay forgery checks
C++ ABI consumer compile
monolithic 1.20 regression
ordered-octonion I119 regression
exact VM81 kernel self-verification
incremental make c-abi invalidation/relink proof
```

Historical green runs remain evidence for the exact commits they tested only. They are not promoted to this later repair head.

## Current checkpoint

Main-alignment and implementation repairs through the incremental rebuild gate are repository-visible through commit:

`e37cfa2f86e6af8670f04d9a945f5e74bcb84882`

This restart record is a later additive checkpoint. Fresh workflow results for the final head are required before classification as frozen/validated.

## Next action

1. compare the final branch head to canonical `main` and require merge base exactly `f5d8fdc...`, zero behind;
2. inspect dependency-scoped exact/synthetic workflow execution;
3. repair only observed failures;
4. resolve PR review threads only when the corresponding repair is present and validated;
5. update PR #315 with final checkpoint/evidence;
6. keep PR #315 unmerged until separate explicit integration authorization is given.
