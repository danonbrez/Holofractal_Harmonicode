# Pass 219 — Prime-Memristive Fifth Hydration Lane Index/Intersection Cycle I2

Status: **ADDITIVE / EXACT-INTEGER / CANDIDATE-ONLY / NON-CANONICAL ROUTING INDEX**

Predecessor: `PASS_219_PRIME_MEMRISTIVE_FIFTH_LANE_V1.md`

Base checkpoint: `agent/pass219-prime-memristive-fifth-lane-i1-20260912 @ 46b7ae3de8f1c6cd89a48043ae2b21f9b53d9fec`

## 1. Purpose

Iteration 2 turns the fifth hydration lane from a per-state modular fingerprint/router into a bounded graph-addressing surface over many hydrated records.

The cycle MUST provide an alternate access path to the inherited Hash216/VM81 knowledge graph without creating a second transition, hash, or persistence authority.

The target relation is:

```text
existing Holo4 / RNA prepared 81-cell candidate
-> deterministic Lane-5 cellular fingerprint
-> selected prime-fibre circuit coordinates
-> inverted posting lookup
-> progressive exact intersection
-> bounded candidate set
-> inherited Hash216 / VM81 final admission
```

## 2. Inherited prime manifold

The I1 prime manifold remains unchanged:

```text
5184 = 2^6 * 3^4
P5 = consecutive primes 5..331
|P5| = 65
Q5 = product(P5)
gcd(Q5, 5184) = 1
Q5 < 72^72 < 337 * Q5
```

Iteration 2 MUST NOT add 2 or 3 to the fifth-lane prime basis and MUST NOT change the four inherited hydration lanes.

## 3. Holo4 / RNA prepared-state adapter

The adapter consumes the existing `HHSExactPass219Holo4PreparedV1` 81-cell surface. It derives one exact signed-63-bit cell scalar per prepared cell from already-emitted integer cell evidence, including the inherited local signature, population counts, Hash216 positions, and Lo Shu coordinates.

The adapter MUST:

- be deterministic for byte-identical prepared input;
- preserve the existing `source_transition_identity216` only as a non-authoritative reference signature;
- call the inherited I1 fingerprint implementation rather than duplicate the modular folding equations;
- emit candidate-only metadata;
- never mutate the prepared state or VM81 frame.

## 4. Circuit index key

For every record and every prime fibre `p`, the index key is the I1 circuit configuration coordinate:

```text
K = (fibre_index, u, v, rho)
```

where the fibre index fixes `p` and `(u,v,rho)` is emitted by the I1 modular cellular fingerprint.

The index is an inverted map:

```text
K -> sorted unique record IDs
```

No posting key is a canonical identity. Hash216/VM81 identity remains external to the index.

## 5. Progressive candidate intersection

Given an inherited I1 route decision with ordered fibre coordinates

```text
K0, K1, ..., Km-1
```

Iteration 2 resolves:

```text
C0 = posting(K0)
Ci = C(i-1) INTERSECT posting(Ki)
```

using exact sorted-integer intersection.

The search stops only when one of the following exact conditions is true:

```text
|Ci| <= candidate_budget
no posting exists for Ki
all selected route axes have been consumed
```

`candidate_budget` MUST be a positive integer. No probabilistic confidence threshold may replace the exact candidate-count gate.

## 6. Linear traversal oracle

Iteration 2 retains a linear scan over the same activated axes as an acceptance oracle.

For a completed progressive query using `a` axes:

```text
indexed_candidate_IDs == linear_scan_candidate_IDs
```

is mandatory.

Benchmark counters MUST be structural counters, not wall-clock authority:

```text
posting lookups
posting entries examined
intersection comparisons
linear record visits
linear coordinate comparisons
```

Any measured speedup is workload evidence only and MUST NOT be promoted to a universal complexity or latency claim.

## 7. Route-plan cache

The selected prime-fibre order may be stored as reusable candidate-only route metadata.

The cached object contains only the selected fibre indices and a deterministic non-cryptographic plan signature. It MUST NOT cache or mint canonical Hash72/Hash216 receipts and MUST NOT commit state.

## 8. Learning boundary

Successful or unsuccessful retrieval MAY feed trinary evidence into the inherited I1 memristive conductance update.

Learning changes accessibility metadata only:

```text
knowledge state != route-to-knowledge state
```

Iteration 2 MUST NOT write the knowledge object, VM81 frame, Hash72 state, Hash216 state, or persistent canonical store.

## 9. Required authority flags

Every new I2 record, query result, linear result, and route plan MUST state:

```text
candidate_only = true
exact_integer_only = true
canonical_mutation_authority = false
canonical_hash72_authority = false
canonical_hash216_authority = false
canonical_persistence_authority = false
floating_point_authority = false
requires_inherited_vm81_hash216_admission = true
```

## 10. Iteration-2 acceptance workload

The first implementation test MUST include:

1. one real `HHSExactPass219Holo4PreparedV1` generated through the inherited C ABI and adapted twice with deterministic equality;
2. a deterministic corpus of at least 4096 Lane-5 fingerprints;
3. neutral high-prime routing using the inherited I1 router;
4. progressive indexed lookup with exact candidate-budget stopping;
5. equality with linear traversal over the same used axes;
6. an alternate learned low-prime route beginning `5 -> 7 -> 11` that converges to the same target record;
7. route-plan cache replay;
8. candidate-local feedback with inherited I1 authority flags preserved;
9. dependency-scoped replay of the I1 fifth-lane test and inherited Holo4 exact ABI build.

The acceptance workload SHOULD demonstrate fewer posting entries examined than linear record visits, but the correctness condition is candidate-set equality, not performance magnitude.

## 11. Forward cycle

After I2 is green, the next cycle may bind the candidate index to reusable Hash216 cache references and query-context route composition, but it MUST retain final canonical admission in the inherited VM81/Hash216 authority chain.
