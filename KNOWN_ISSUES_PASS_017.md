# KNOWN ISSUES PASS 017

1. Full TypeScript compilation still requires installing frontend Node dependencies.
2. Some GUI components still consume non-runtime API endpoints directly.
3. Contract validation in the frontend is observational only; authoritative enforcement remains backend/kernel-side by design.
