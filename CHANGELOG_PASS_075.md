# Changelog — Pass 075

Added the native `hhs_harmonicode_language` product with a deterministic parser, `HHS_TYPED_IR_V1`, validation, symbol indexing, lineage-gated IR commitment, agent-coordinated test acceleration, unified API/CLI projections, canonical artifacts, and context-independent replay.

Pass 072 and Pass 074 parent files remain byte-identical.
