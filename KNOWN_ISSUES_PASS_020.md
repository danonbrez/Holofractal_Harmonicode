# KNOWN ISSUES — PASS 020

- The closure harness is an operational integration harness, not a full mathematical proof engine.
- The normalized signature intentionally excludes live UUIDs, timestamps, ledger height, and current runtime step hash to avoid false drift from transport metadata.
- The current vector projection in the harness is a compact deterministic test vector, not the final modality embedding engine.
- The GUI does not yet expose a dedicated closure harness panel.
- Ledger files generated during local verification should not be treated as release authority artifacts; they are runtime artifacts.
