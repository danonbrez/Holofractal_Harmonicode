# Pass 205 Next Implementation Boundary

The next implementation step is the production continuation ABI and one physical accelerator backend:

1. native fixed-width state, delta, continuation, projection, and accelerator-buffer records;
2. production Hash216/Hash72/VM81 bindings;
3. persistent snapshot and lineage store;
4. one deterministic CUDA, HIP, Vulkan compute, WebGPU, or Metal implementation;
5. exact CPU/GPU differential validation;
6. hosted continuation API and visual graph controls.
