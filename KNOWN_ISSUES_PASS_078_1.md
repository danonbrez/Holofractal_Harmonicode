# Known Issues Pass 078.1

The fifteen `hhs_vm_*` declarations remain intentionally non-callable. Closing them requires a versioned architectural revision proving state-layout, program, receipt, ownership, mutation, and failure-semantic equivalence or implementing a new explicitly versioned native ABI.
