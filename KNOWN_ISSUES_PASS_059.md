# Known Issues — Pass 059

- The reference transaction coordinator is deterministic and in-process; transport adapters remain external.
- Compensation semantics are explicit records and do not infer domain-specific inverse operations.
