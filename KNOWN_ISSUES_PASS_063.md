# Known Issues — Pass 063

- The pass executes the committed Pass 062 algebra contract; broader source-language ingestion remains bounded by the existing parser/operator registry.
- Conflict localization is deterministic and typed; richer multi-conflict scheduling is deferred.
