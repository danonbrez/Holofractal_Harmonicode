# Workspace GUI Component Map — Pass 049

The GUI mounts `HHSWorkspaceShell` inside `RuntimeShell` and includes project tree, ingress, symbolic editor, interpreter, compiler, emulator, graph, semantic memory, ledger, history, inspector, and authority status panels. The frontend registry is projection-only.
