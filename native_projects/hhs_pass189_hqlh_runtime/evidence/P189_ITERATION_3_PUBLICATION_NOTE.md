# Pass 189 Iteration 3 publication note

The source manifest binds publication identity to the authoritative Git commit and tree. Its prepublication aggregate records the independently validated local artifact set; GitHub Actions reruns the complete inherited `make validate` against the exact proposed tree before merge.

The compact registry and deployment-file rewrites preserve inherited semantic fields and service authority while adding Iteration 3. No Pass 190 path is changed.
