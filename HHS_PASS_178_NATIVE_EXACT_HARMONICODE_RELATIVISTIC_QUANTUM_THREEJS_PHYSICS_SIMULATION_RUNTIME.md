# HHS PASS 178 — NATIVE EXACT HARMONICODE RELATIVISTIC AND QUANTUM THREE.JS PHYSICS SIMULATION RUNTIME

## Native No-Float Simulation Authority, Exact Symbolic and Algebraic Constraint Evolution, VM81-Registered ABI Execution, Hash72/Hash216 State Receipts, Relativistic Particle and Quantum Wave Dynamics, Deterministic Three-Dimensional Projection, Scientific Validation, Replay, Capture, and Visual IDE Integration

# 1. Normative metadata

| Field | Normative value |
|---|---|
| Contract identifier | `HHS-P178-NEH-RQ-TJS-PSR` |
| Pass number | `178` |
| Canonical pass name | `NATIVE_EXACT_HARMONICODE_RELATIVISTIC_QUANTUM_THREEJS_PHYSICS_SIMULATION_RUNTIME` |
| Short name | `P178 Exact Physics Studio` |
| Contract version | `1.0.0` |
| Repository | `danonbrez/Holofractal_Harmonicode` |
| Contract grounding baseline | Authoritative `main @ 28a298e296ae27d13c3b5c5c51271a250cbeb42a` |
| Immediate pass parent | Complete Pass 177 universal application and creative-content workflow system, including the native platformer architecture and every compatible append-only amendment |
| Kernel inheritance | Complete Pass 175 Hash216-hydrated VM5184 × G243 virtual instruction processor and singleton VM81 admission authority |
| Visual inheritance | Complete Pass 176 frozen production multimodal IDE and Pass 177 project/template/workflow surfaces |
| Physics execution authority | Native Harmonicode exact algebra and one registered native API/ABI runtime path |
| Rendering authority | Three.js/WebGL projection only; never simulation, collision, measurement, or commit authority |
| Canonical state authority | Singleton VM81 admission membrane followed by the singular ordered Hash72 commit clock |
| Canonical identity | Hash216-bound source, model, constraint graph, transition, snapshot, render packet, evidence, and replay identities |
| Authoritative numeric rule | No IEEE-754 floating-point value may enter authoritative simulation state or influence an admitted transition |
| Initial classification | `CONTRACT AUTHORIZED — IMPLEMENTATION GATED BY PASS 177 TERMINAL CLOSURE` |

# 2. Normative language

The terms **SHALL**, **SHALL NOT**, **MUST**, **MUST NOT**, **SHOULD**, **SHOULD NOT**, and **MAY** are normative.

Pass 178 defines a scientific simulation and visualization architecture. It does not declare every HHS symbolic relation to be an experimentally established law of nature. Standard relativistic, quantum, electromagnetic, thermodynamic, and conservation equations SHALL be identified separately from HHS-specific admissibility constraints. HHS constraints MAY govern model state, indexing, closure, replay, and symbolic correspondence, but SHALL NOT be presented as empirical replacements for established physical equations without independent evidence.

# 3. Append-only inheritance

Pass 178 SHALL inherit every compatible repository-visible contract, implementation, test, schema, ABI, API, compiler surface, visual workflow, deployment rule, receipt, and correction through Pass 177 as its minimum foundation.

It SHALL preserve, without weakening:

- exact Harmonicode source authority;
- `O != Pi` and `O ≠ Π` as a semantic distinction;
- exact integer, rational, symbolic, algebraic-root, complex, matrix, tensor, and typed-boundary representations;
- singleton VM81 admission and one ordered Hash72 commit authority;
- Hash216 instruction, state, artifact, and replay identity;
- deterministic fixed-step execution;
- restartability from repository-visible state;
- dependency-scoped validation and repair-forward delivery;
- incremental Git history containing only new or modified source-oriented files;
- the visual IDE as the principal product surface;
- natural-language assistance, source editing, visual controls, file handling, preview, testing, build, export, and human-readable evidence;
- production accessibility, mobile interaction, security, observability, recovery, and deployment requirements.

No Pass 178 component may bypass an inherited authority because a direct JavaScript, Three.js, WebGL, worker, WebAssembly, Python, shell, or private endpoint appears faster or more convenient.

# 4. Required result

Pass 178 SHALL deliver a production-capable **Relativistic Quantum Simulation Studio** that:

1. accepts Harmonicode, structured model declarations, visual parameter editing, and natural-language model requests;
2. compiles each model into a typed exact constraint graph;
3. registers every executable model through one native API/ABI registry;
4. evolves authoritative state using exact integer, rational, symbolic, algebraic, complex, matrix, and tensor operations;
5. uses VM81 admission before any transition becomes authoritative;
6. emits one ordered Hash72 receipt stream and Hash216 identities;
7. projects admitted snapshots into Three.js without feeding renderer values back into simulation;
8. provides realistic three-dimensional animation, camera control, lighting, materials, trails, vector fields, spacetime grids, wave amplitude, phase, probability, and diagnostic overlays;
9. provides at least one relativistic charged-particle simulation and one quantum wave-packet/double-slit simulation;
10. verifies conservation, domain, branch, normalization, and closure constraints;
11. supports deterministic pause, single-step, scrub, replay, comparison, and MP4 capture;
12. remains usable on desktop and mobile;
13. exposes expert source and proof inspection without requiring ordinary users to understand VM81 or Hash216;
14. produces complete machine-readable and human-readable evidence;
15. is browser-tested end to end against the served application.

# 5. Binding architecture

```text
Harmonicode source / model template / visual controls / assistant request
        ↓
byte-preserving source ingress
        ↓
typed CST
        ↓
typed AST
        ↓
constraint relation graph
        ↓
physics HIR
        ↓
VM81 exact simulation IR
        ↓
native API/ABI model registration
        ↓
exact initial-state admission
        ↓
fixed-step exact transition candidate
        ↓
domain, branch, conservation, and HHS membrane validation
        ↓
singleton VM81 admission
        ↓
singular Hash72 commit
        ↓
Hash216 snapshot and replay identity
        ↓
immutable quantized render packet
        ↓
Three.js scene projection and GPU rasterization
```

The authoritative direction is one way. Renderer state SHALL NOT flow backward across the render-packet boundary.

# 6. Authority separation

The following identities are binding:

\[
\boxed{\text{simulation state}\neq\text{render state}}
\]

\[
\boxed{\text{unitary evolution}\neq\text{measurement selection}\neq\text{visual particle interpretation}}
\]

\[
\boxed{\text{HHS admissibility constraint}\neq\text{experimentally validated physical law}}
\]

\[
\boxed{\text{GPU coordinate}\neq\text{authoritative physical coordinate}}
\]

Three.js SHALL own scene composition, camera, materials, post-processing, interpolation between admitted snapshots, and visual effects. It SHALL NOT own integration, collision or boundary decisions, relativistic proper time, quantum evolution, measurement selection, probability normalization, conservation acceptance, HHS closure, VM81 admission, Hash72 ordering, Hash216 identity, or replay truth.

# 7. Exact numeric domain

The authoritative runtime SHALL support at least:

```text
ExactInteger
ExactRational
PrimeExponentRatio
SymbolicConstant
SymbolicFunction
AlgebraicRoot
CertifiedInterval
ComplexExact
ExactVector
ExactMatrix
ExactTensor
TypedResidue
TypedZero
FoldReciprocal
BoundaryInfinity
ConstraintProposition
ConstraintWitness
BranchWitness
UnitWitness
DimensionWitness
```

## 7.1 No-float rule

`float`, `double`, JavaScript `Number`, GPU scalar values, and untyped decimal approximations SHALL NOT be admitted into authoritative state.

A decimal source token SHALL be retained lexically and classified as an exact rational decimal, named symbolic constant, bounded certified interval, or display-only approximation explicitly excluded from authority.

Source spellings such as `3.14159265359` and `2.71828182846` SHALL NOT silently replace `Pi` or `E`, and `O` SHALL remain distinct from both.

## 7.2 Transcendental values

`Log`, `Exp`, trigonometric functions, and other transcendental operations SHALL remain symbolic when exact closure is possible symbolically. When a finite enclosure is required, it SHALL use a deterministic certified interval with explicit precision, rounding direction, and error witness. An interval may support an admission proof only when the proof remains valid over the full interval.

## 7.3 Units and dimensions

Every physical scalar SHALL carry a unit/dimension witness or belong to an explicitly declared dimensionless system. Addition and equality between incompatible dimensions SHALL be rejected before VM81 admission.

# 8. Source and circuit identity

Harmonicode source SHALL be preserved byte-for-byte before parsing.

A scalar `List(...)` operand is an exact byte-aligned circuit encoding. The runtime SHALL preserve every element, position, duplicate, sign, separator, nesting level, lexical width, leading zero, parenthesis, explicit numerator and denominator, and source symbol distinction.

It SHALL NOT sort list lanes, deduplicate repeated entries, strip leading zeros, coerce lexical decimals to binary floats, reorder noncommutative operands, collapse repeated zeros, shorten a list, substitute `O` for `Pi` or `Π`, or interpret a circuit list as an unordered mathematical set.

The complete user-supplied Pass 178 constraint corpus, beginning with the `P^2` reciprocal-closure chain and ending with the modular unit-circle relation `t^(x*u)/(x*y)==(-x*y)==E^(I*Pi)`, SHALL be persisted byte-for-byte as a versioned source artifact during implementation before any normalization or compilation. Its source SHA-256 and Hash216 identity SHALL be included in the implementation manifest.

# 9. Constraint-program semantics

The supplied large expression SHALL be compiled as a simultaneous typed constraint program, not as one eager scalar calculation and not as a sequence of JavaScript Boolean comparisons.

Each `==` SHALL create a typed relation edge recording left and right operand identity, type, unit, dimension, domain, branch, local layer, inherited constraints, global constraints, provenance, admission status, and witness or rejection reason.

For candidate state \(S\) in local layer \(L\):

\[
\operatorname{False}_{HHS}(S,L)
\iff
\neg I_{\mathrm{global}}(S)
\lor
\neg I_{\mathrm{inherited}}(S,L).
\]

An apparent local contradiction MAY be classified as superposition, opposed phase lanes, a modular pivot, a nesting fold, or unresolved state when no active invariant is violated. Such classification SHALL remain typed and SHALL NOT silently become ordinary scalar truth.

# 10. Canonical HHS membrane

The Pass 178 global admission membrane SHALL include:

\[
\boxed{P^4=AB}
\]

\[
\boxed{\Delta=P^2-pq}
\]

\[
\boxed{A=B=P^2}
\]

\[
\boxed{u^{72}=1}
\]

\[
\boxed{O\neq\Pi}
\]

with branch and domain witnesses wherever square roots occur.

On the admitted nonnegative branch:

\[
\sqrt{AB}=\sqrt{BA}=P^2.
\]

The source relation `A=P=B` SHALL be typed. Unless an explicit scalar domain proves otherwise, it SHALL mean shared register or lane authority:

\[
\operatorname{register}(A)=\operatorname{register}(P)=\operatorname{register}(B),
\]

while numeric payload closure remains:

\[
\operatorname{value}(A)=\operatorname{value}(B)=P^2.
\]

No compiler pass may conflate register identity with unrestricted scalar equality.

The coupled information carrier SHALL be the transition:

\[
\Gamma_n:(P_n,s_n,f_n)\rightarrow(P_{n+1},s_{n+1},f_{n+1}),
\]

not any isolated instantaneous value. Ordering, provenance, transition size, recurrence period, and constraint witnesses are part of state identity.

# 11. Typed zero, phase pivot, and boundary infinity

Pass 178 SHALL distinguish:

```text
ScalarZero
ResidueZero(modulus)
PhasePivotZero(layer)
RenewedUnit(period)
FoldReciprocalZero(orientation)
BoundaryInfinity(projection)
```

The relation

\[
0_{\bmod 72}=1_{u^{72}}
\]

MAY be admitted only as a typed cyclic closure relation. It SHALL NOT imply unrestricted scalar `0 == 1`.

The typed fold reciprocal

\[
0^{-1}=\operatorname{PhaseRotate}_{M\rightarrow I}(0_L)
\]

SHALL NOT inherit the ordinary field axiom \(0\cdot0^{-1}=1\) unless a separately typed phase-closure operation establishes that relation.

`ComplexInfinity` SHALL be represented as a boundary carrier, not a finite scalar and not a raw argument to ordinary modular arithmetic.

For a declared finite projection witness:

\[
\ComplexInfinity\bowtie\chi_0,
\]

\[
\chi_0=x+y=c^2-b^2-a^2=u^{(0\bmod72)},
\]

the valid modular operation is:

\[
\operatorname{Mod}\left(\Pi_{\chi_0}(\ComplexInfinity),m\right).
\]

Direct `Mod(ComplexInfinity,m)` SHALL be rejected.

# 12. Exact thermodynamic kernel

At fixed declared local volume, Pass 178 SHALL implement the symbolic kernel:

\[
\sigma(u)=-\ln(1-u),
\]

\[
\varepsilon(u)=\frac{1}{1-u}-1+\ln(1-u),
\]

\[
\frac{d\sigma}{du}=\frac{1}{1-u},
\qquad
\frac{d\varepsilon}{du}=\frac{u}{(1-u)^2},
\]

therefore:

\[
\boxed{\frac{d\varepsilon/du}{d\sigma/du}=\frac{u}{1-u}=\theta}
\]

and:

\[
\boxed{d\varepsilon=\theta\,d\sigma}.
\]

The real-domain membrane SHALL require \(1-u\neq0\) and, for the default real thermodynamic profile, \(0\le u<1\). Complex profiles SHALL declare a logarithm branch and winding witness.

Define:

\[
\Phi(G)=G-1-\ln G.
\]

With a branch-consistent reciprocal logarithm:

\[
\Phi(G)+\Phi(G^{-1})=G+\frac1G-2=\boxed{\frac{(G-1)^2}{G}}.
\]

Real default admission SHALL require \(G>0\). Complex admission SHALL require explicit proof that reciprocal logarithm branches cancel as declared, including any \(2\pi i k\) winding contribution.

The local geometric relative-entropy quantities SHALL be available as exact symbolic nodes:

\[
G_k=\frac{1}{1-u_k},
\quad
\sigma_k=\ln G_k,
\quad
\theta_k=G_k-1=\frac{u_k}{1-u_k},
\quad
\varepsilon_k=G_k-1-\ln G_k,
\]

with the first-law relation:

\[
\delta\varepsilon_k=\theta_k\,\delta\sigma_k-\pi_k\,\delta v.
\]

# 13. Relativistic mechanics kernel

The initial relativistic model SHALL include:

\[
c^2d\tau^2=c^2dt^2-dx^2-dy^2-dz^2,
\]

\[
\gamma=\left(1-\frac{v^2}{c^2}\right)^{-1/2},
\]

\[
E^2-p^2c^2=m^2c^4,
\]

\[
\frac{dp^\mu}{d\tau}=qF^\mu{}_{\nu}u^\nu.
\]

Required features include exact or symbolic Minkowski metric selection, exact rational initial conditions, timelike/null/spacelike classification, \(v^2<c^2\) admission for massive particles, separate proper and coordinate time, field-tensor construction, charged-particle trajectories, exact conservation witnesses where available, deterministic boundary conditions, and rejected-state evidence for superluminal or dimensionally invalid inputs.

Square roots SHALL remain algebraic-root nodes or certified intervals. No approximate renderer velocity may influence the relativistic solver.

# 14. Quantum evolution kernel

The first quantum model SHALL use a finite VM81 Hamiltonian and an exact implicit update such as:

\[
\left(I+\frac{i\Delta t}{2\hbar}H\right)\psi_{n+1}
=
\left(I-\frac{i\Delta t}{2\hbar}H\right)\psi_n.
\]

The authoritative runtime SHALL validate:

\[
H=H^\dagger,
\]

\[
\langle\psi_n|\psi_n\rangle=\langle\psi_{n+1}|\psi_{n+1}\rangle,
\]

\[
\sum_k|\psi_k|^2=1.
\]

Required model features include exact complex amplitudes, configurable finite lattice or VM81 cell mapping, wave-packet initialization, free propagation, barrier and double-slit potentials, amplitude/phase/probability/current outputs, unitary evolution separated from measurement, deterministic seeded measurement only through a registered authority, explicit boundary-condition identity, and replayable receipts.

Non-Hermitian Hamiltonians SHALL be rejected unless a specifically typed open-system model is selected.

# 15. Native API/ABI registry

Every Pass 178 operation SHALL enter through one registered native public authority. The minimum C ABI surface SHALL include functionally equivalent operations to:

```c
hhs178_runtime_open(...)
hhs178_model_register(...)
hhs178_source_ingest_exact(...)
hhs178_constraint_bind(...)
hhs178_parameter_set_exact(...)
hhs178_initial_state_admit(...)
hhs178_step_candidate(...)
hhs178_step_validate(...)
hhs178_step_commit(...)
hhs178_snapshot_vm81(...)
hhs178_render_packet_project(...)
hhs178_replay_open(...)
hhs178_replay_step(...)
hhs178_measure_registered(...)
hhs178_receipt_export(...)
hhs178_runtime_close(...)
```

Final names MAY follow the repository ABI convention, but all functions SHALL be versioned, documented, testable, and available through the public registry.

No browser component may invoke private solver internals. Python and JavaScript bindings SHALL call the same ABI authority and demonstrate parity with native C behavior.

# 16. Exact transition clock

Simulation SHALL use an exact fixed-step clock:

\[
t_n=t_0+n\Delta t,
\qquad n\in\mathbb Z_{\ge0},
\]

where \(t_0\) and \(\Delta t\) are exact values.

Wall-clock time, animation-frame timing, device refresh rate, and GPU frame rate SHALL NOT alter the authoritative sequence.

Rendering MAY interpolate visually between committed states, but interpolation SHALL be labeled non-authoritative and SHALL never become a new physical state.

# 17. VM81, Hash72, and Hash216 state path

Each candidate transition SHALL include model identity, source identity, prior state identity, exact step index, exact time, parameter set, constraint graph root, solver operation, candidate state, conservation residuals, branch witnesses, domain witnesses, unit witnesses, HHS membrane witnesses, and admission result.

Only admitted transitions SHALL receive:

1. a VM81 admission record;
2. the next singular Hash72 commit position;
3. a Hash216 transition identity;
4. a replay index;
5. an immutable render packet.

Rejected transitions SHALL receive non-authoritative diagnostic evidence without advancing the authoritative simulation clock.

# 18. Immutable render packet

The native runtime SHALL emit a versioned immutable packet containing integer, rational, fixed-point, indexed, or text-safe projections such as:

```text
step_index
world_time_num
world_time_den
proper_time_num
proper_time_den
position_q32_32[3]
orientation_q2_62[4]
velocity_projection_q32_32[3]
phase_index_mod_72
amplitude_num
amplitude_den
probability_num
probability_den
field_vector_q32_32[]
state_hash216
transition_hash216
render_projection_error_bound
authoritative_snapshot_reference
```

The exact packet schema SHALL specify overflow behavior, scaling, endian order, bounds, and conversion rules.

Three.js SHALL receive render packets through a read-only adapter. Any attempt to mutate an authoritative packet or feed a renderer-derived value into simulation SHALL be rejected and tested.

# 19. Three.js visual system

The Three.js surface SHALL provide perspective and orthographic cameras, orbit/track/follow/spacetime camera modes, responsive mouse/keyboard/touch/controller input, physically intelligible lighting and materials, scalable geometry and instancing, trajectory/world-line trails, electric and magnetic vector overlays, spacetime grids and light cones, quantum amplitude-height surfaces, phase encoding with an accessible non-color alternative, probability-density volume/surface/point-cloud projections, configurable quality profiles, camera-safe mobile controls, deterministic screenshot/frame capture, and a visible distinction between authoritative snapshots and interpolated render frames.

Post-processing MAY improve legibility and realism but SHALL be optional and SHALL not obscure diagnostics or change simulation results.

# 20. Required simulation templates

## 20.1 Relativistic charged-particle laboratory

The template SHALL support particle mass and charge, exact initial four-position and four-momentum, uniform and spatially varying electric/magnetic fields, proper-time and coordinate-time views, trajectory/velocity/momentum/force/field overlays, Newtonian comparison mode, invariant residual display, rejected superluminal demonstrations, deterministic playback, and capture.

## 20.2 Quantum double-slit laboratory

The template SHALL support exact wave-packet parameters, lattice and time-step configuration, slit geometry and potential, amplitude and phase views, probability density and current, normalization witness, unitary replay, optional registered measurement sampling, comparison of slit configurations, deterministic playback, and capture.

## 20.3 Harmonicode constraint laboratory

The template SHALL expose the exact source, parsed constraint graph, `(P,s,f)` transition lanes, \(P^4=AB\), \(\Delta=P^2-pq\), and \(u^{72}=1\) witnesses, typed zero and boundary-infinity states, Lo Shu and VM81 cell projections, accepted/rejected/unresolved/superposed/folded classifications, and source-to-render provenance.

# 21. Visual IDE integration

Pass 178 SHALL be integrated into the inherited main IDE surface, not delivered as a detached demo.

The IDE SHALL include a Physics Studio project template, model and scene file tree, Harmonicode editor, visual parameter inspector, constraint graph inspector, native runtime status, VM81 register view, Hash72 timeline, Hash216 snapshot browser, Three.js viewport, simulation transport controls, timeline scrubber, branch/domain/unit/conservation diagnostics, replay comparison, evidence export, MP4 capture workflow, and natural-language assistant capable of creating and modifying models through public governed operations.

Raw JSON MAY be available in an expert diagnostics view but SHALL NOT be the default interface.

# 22. Scientific evidence and labeling

Every simulation SHALL display model equations, assumptions, units, domain restrictions, boundary conditions, method, time step, exactness classification, projection error bound, conservation status, HHS constraint status, and authoritative versus visual-only values.

Claims SHALL be classified as:

```text
STANDARD_PHYSICS_EQUATION
HHS_ADMISSIBILITY_CONSTRAINT
DERIVED_SYMBOLIC_IDENTITY
NUMERICAL_OR_INTERVAL_APPROXIMATION
RENDER_ONLY_INTERPRETATION
UNRESOLVED_HYPOTHESIS
```

The interface and exported report SHALL not blur these classifications.

# 23. Deterministic replay and capture

A replay capsule SHALL contain repository commit, model source, source hash, compiled graph identity, ABI/runtime version, exact initial state, exact parameters, exact clock, all admitted transition identities, measurement seed and authority when used, render-packet schema, camera script, quality profile, capture settings, and final evidence roots.

MP4 capture SHALL be generated from the same ordered admitted state stream used by interactive playback. A capture that merely resembles the simulation without replaying its authoritative stream SHALL not satisfy this contract.

# 24. Performance requirements

Exact authority SHALL not be weakened for speed.

The implementation SHALL use immutable snapshot reuse, structural sharing, sparse VM81/lattice updates, cached symbolic subexpressions keyed by exact identity, bounded certified-interval refinement, pooled render objects, instanced geometry, frustum and distance culling, adaptive render quality, separation of simulation and render rates, bounded memory/queue growth, and explicit backpressure.

The UI SHALL remain responsive while simulation work is in progress. Long operations SHALL expose progress, cancellation where safe, checkpointing, and restart instructions.

Performance reports SHALL separately measure exact transition throughput, validation throughput, receipt/Hash216 overhead, render-packet projection, Three.js frame rate, capture throughput, and mobile memory usage. Renderer FPS SHALL never be reported as solver throughput.

# 25. Required validation matrix

At minimum, Pass 178 SHALL execute:

## 25.1 Exact algebra

- integer and rational closure;
- symbolic cancellation;
- algebraic-root branch handling;
- certified-interval containment;
- complex conjugation;
- exact matrix/tensor operations;
- source lexical preservation;
- exact `List(...)` circuit preservation;
- `O != Pi` enforcement;
- floating-point ingress rejection.

## 25.2 Constraint membrane

- positive \(P^4=AB\) and \(\Delta=P^2-pq\) cases;
- negative mismatched-`A/B` cases;
- register-identity versus scalar-equality cases;
- typed zero closure;
- fold reciprocal;
- boundary-infinity projection;
- direct infinity-modulus rejection;
- inherited/global contradiction classification.

## 25.3 Thermodynamics

- symbolic derivative identities;
- reciprocal \(\Phi\) closure;
- real-domain rejection at \(u=1\);
- logarithm branch witnesses;
- first-law relation checks.

## 25.4 Relativity

- inertial motion;
- constant-field charged-particle motion;
- proper-time monotonicity;
- mass-shell conservation;
- superluminal input rejection;
- unit mismatch rejection;
- Newtonian-limit comparison.

## 25.5 Quantum

- Hermiticity;
- norm conservation;
- free packet propagation;
- barrier interaction;
- double-slit interference;
- boundary-condition replay;
- deterministic measurement receipts;
- non-Hermitian rejection in closed-system mode.

## 25.6 ABI and replay

- C/Python/JavaScript parity;
- public-registry-only enforcement;
- candidate rejection without clock advancement;
- deterministic cold replay;
- interrupted-run recovery;
- exact snapshot identity;
- tamper detection;
- cross-platform serialization.

## 25.7 Browser and graphics

- desktop and mobile controls;
- no frozen interface;
- no console errors;
- viewport resize;
- quality scaling;
- authoritative/interpolated status labels;
- screenshot and MP4 generation;
- representative complete playthroughs of all three templates.

# 26. Security and safety

Pass 178 SHALL sandbox imported model code, reject unauthorized native calls, limit resource use, validate tensor/allocation bounds, prevent shader or asset injection from acquiring solver authority, require explicit authorization for filesystem/network/deployment/external-device operations, preserve provenance, distinguish educational simulation from engineering certification, and warn where a model is not suitable for safety-critical prediction.

# 27. Required repository artifacts

Terminal implementation SHALL include at least:

```text
HHS_PASS_178_NATIVE_EXACT_HARMONICODE_RELATIVISTIC_QUANTUM_THREEJS_PHYSICS_SIMULATION_RUNTIME.md
contracts/pass178/HARMONICODE_PHYSICS_CONSTRAINT_CORPUS.hhs
contracts/pass178/PASS_178_MACHINE_CONTRACT.json
native_projects/hhs_pass178_exact_physics/
hhs_runtime/pass178/
applications/holofractal_harmonizer/src/physics-studio/
tools/capture-pass178-physics.py
tests/test_hhs_pass178_exact_algebra.*
tests/test_hhs_pass178_relativity.*
tests/test_hhs_pass178_quantum.*
tests/test_hhs_pass178_abi_replay.*
tests/test_hhs_pass178_browser.*
docs/PASS_178_PHYSICS_STUDIO_TUTORIAL.md
evidence/pass178/
```

Equivalent repository-consistent paths MAY be used, but every functional category SHALL be present.

# 28. Completion gates

Pass 178 is terminal only when all of the following are true:

1. the complete exact source corpus is repository-visible and hash-bound;
2. the parser preserves circuit identity;
3. no authoritative transition depends on IEEE-754 input;
4. all physics models execute through the registered native API/ABI;
5. VM81 admission and the singular Hash72 commit path are enforced;
6. Hash216 identities and deterministic replay are complete;
7. the relativistic, quantum, and Harmonicode laboratories are functional;
8. Three.js receives read-only immutable render packets;
9. the IDE provides usable desktop and mobile workflows;
10. conservation, branch, domain, unit, and HHS constraints are visible;
11. browser playthroughs finish without frozen controls or console errors;
12. deterministic MP4 evidence is generated;
13. dependency-scoped native, binding, replay, and browser tests pass;
14. implementation and recovery state are repository-visible;
15. all work is committed to authoritative `main`, or a ready-to-merge PR is created and immediately reported;
16. authoritative `main` is verified after merge;
17. a full human-readable implementation report and tutorial are committed.

# 29. Non-completion conditions

Pass 178 SHALL be classified incomplete when any of the following is true:

- Three.js directly computes authoritative physics;
- JavaScript or GPU values feed back into the solver;
- floats are stored in authoritative state;
- a private endpoint bypasses the public ABI registry;
- the large expression is reduced to an eager Boolean or ordinary scalar calculation;
- `List(...)` circuit tokens are normalized, reordered, shortened, or stripped;
- `O` and `Pi` are conflated;
- typed zero collapses unrestricted arithmetic into `0 == 1`;
- `ComplexInfinity` is passed directly into ordinary `Mod`;
- standard physical laws and HHS constraints are presented without classification;
- unitary evolution and measurement are conflated;
- renderer interpolation is committed as physical truth;
- evidence cannot cold-replay;
- the interface is a static shell, raw-JSON viewer, or disconnected demo;
- mobile controls are unusable;
- capture is not derived from the admitted state stream;
- work remains uncommitted or stranded on an unmerged branch.

# 30. Restartability and delivery

Every implementation task SHALL record authoritative repository and base commit, branch and merge target, changed files, commands executed, completed validations, remaining validations, environment/deployment state, exact next action, and blockers before risky or long-running work.

Closure SHALL follow:

```text
IMPLEMENT
→ DEPENDENCY-SCOPED VALIDATION
→ COMMIT
→ MERGE OR OPEN READY PR
→ VERIFY MAIN
→ RETURN COMPLETION STATUS
```

Repeated execution of unchanged broad test suites SHALL not replace dependency-scoped validation. Previously verified evidence MAY be frozen until an affected dependency changes. Later defects SHALL be repaired forward.

# 31. Terminal classification

Before all completion gates are satisfied:

```text
HHS_PASS_178_EXACT_PHYSICS_STUDIO_INCOMPLETE
```

After all completion gates are satisfied:

```text
HHS_PASS_178_NATIVE_EXACT_HARMONICODE_RELATIVISTIC_QUANTUM_THREEJS_PHYSICS_SIMULATION_RUNTIME_VERIFIED
```

The terminal receipt SHALL bind contract identity, authoritative main commit, exact source-corpus hash, native ABI version, VM81/Hash72/Hash216 evidence roots, validation matrix, browser evidence, MP4 hashes, tutorial, deployment status, and deterministic replay root.

---

## Binding summary

\[
\boxed{\text{Harmonicode computes and proves the state;}\quad\text{VM81 admits and commits the state;}\quad\text{Three.js reveals the state.}}
\]

No lower-authority layer may reverse this order.
