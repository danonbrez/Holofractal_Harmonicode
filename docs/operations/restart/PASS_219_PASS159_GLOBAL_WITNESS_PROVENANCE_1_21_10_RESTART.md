# Pass 219 I121.10 — Pass159 whole-expression witness provenance restart

## Authority

- Repository: `danonbrez/Holofractal_Harmonicode`
- Canonical immutable base: `main @ f5d8fdc014d888f93c0d85d40d2a2c0c198eefdf`
- Active branch: `agent/pass219-orthogonal-glyph-parallel-membrane-1-21`
- Existing PR: `#315`, draft, unmerged
- I121.9 validated semantic head: `9b3bbd5ce087df9875e2b8b9cb4b2d9513fb4b92`
- I121.9 terminal evidence seal / I121.10 parent: `13be0d935458344db30fcdfa43407d2cfca78512`
- No canonical-main merge is authorized.

## Objective

I121.10 binds the complete 632-byte combined Harmonicode equation to the inherited Pass159 whole-expression compiler pipeline as deterministic provenance evidence for the I121.9 global constraint membrane.

It SHALL NOT manufacture Boolean truth, infer that all five `==` gates are true, substitute for Pass169 whole-expression authority, mutate VM81, commit Hash72, persist canonical state, or claim canonical monolithic proof.

The intended boundary is:

```text
exact 632-byte combined source
        ↓
frozen Pass159 source → tokens → CST → AST → types → constraint graph → HIR → VMIR
        ↓
exact source/artifact identities + exact five `==` occurrence offsets
        ↓
deterministic provenance-only shared-environment root
        ↓
I121.10 provenance record
        ↓
Pass169 Boolean/whole-expression proof still required
        ↓
I121.9 membrane input may be formed only after authoritative gate results exist
```

## Exact source binding

Source:

`contracts/pass219/PASS_219_COMBINED_QUOTIENT_MATRIX_POWER_NATIVE_1_21_8.harmonicode`

```text
bytes  = 632
sha256 = 3315641c8d6aa9fc4f3918eccda8e3a40c8445cc417a65e5dea683f68020cf53
UTF-8 `==` byte offsets = 96, 240, 266, 274, 285
literal `=` chars = 16
`==` tokens = 5
equality tokens = 11
```

## Frozen Pass159 evidence already established

The green I121.8 identity census proves that the complete 632-byte equation remains distinct from the 348-byte numerator through every inspected Pass159 stage:

```text
source_equal=0
tokens_equal=0
cst_equal=0
ast_equal=0
types_equal=0
graph_equal=0
hir_equal=0
vmir_equal=0
```

Therefore I121.10 may bind to genuine whole-expression compiler identity rather than a source-only surrogate.

The current frozen Pass159 implementation remains foundation/source-pipeline authority, not candidate-bound canonical VM81 proof. Pass169 remains the required authority for whole-expression constraint truth and VM81 admission/commit.

## Planned implementation

Additive branch-local files only:

- `HHS_PASS_219_APPEND_ONLY_PASS159_GLOBAL_WITNESS_PROVENANCE_AMENDMENT_1_21_10.md`
- `hhs_runtime/include/hhs_pass219_pass159_global_witness_provenance_1_21_10.h`
- `hhs_runtime/c/hhs_pass219_pass159_global_witness_producer_1_21_10.c`
- `tests/pass219/test_pass219_pass159_global_witness_producer_1_21_10.c`
- `.github/workflows/pass219-pass159-global-witness-provenance-1-21-10.yml`
- this restart record

The producer is intentionally separate from the cumulative exact-ABI aggregate because it links the frozen Pass159 library and may use the inherited OpenSSL SHA-256 pattern for a provenance-only root. This avoids adding a new crypto/link dependency to historical standalone exact-ABI consumers.

## Required output classification

The provenance record SHALL expose exact/deterministic compiler identities and source occurrence provenance, and SHALL explicitly report:

```text
pass159_whole_expression_provenance_verified = true
boolean_gate_results_available = false
membrane_input_ready = false
pass169_whole_expression_authority_required = true
canonical_monolithic_proof = false
floating_point_authority = false
vm81_mutation_authority = false
hash72_commit_authority = false
persistence_mutation_authority = false
```

A populated provenance root is an identity binding only. It is not an algebraic result and cannot make a false/unresolved gate true.

## Required validation

1. canonical main / Pass159 / Pass169 / I121.9 ancestry;
2. frozen Pass159 and Pass169 files untouched;
3. I121.9 membrane implementation/source fixture untouched;
4. exact 632-byte SHA and five UTF-8 byte offsets;
5. frozen Pass159 builds unchanged;
6. producer strict C11 compile/link;
7. full source traverses source→tokens→CST→AST→types→graph→HIR→VMIR;
8. source, AST, type, graph, HIR, and VMIR Hash216 identities are populated and deterministic;
9. inherited artifact source/parent-root lineage is internally consistent;
10. provenance-only environment root is deterministic and nonzero;
11. producer never supplies Boolean gate truth and never marks I121.9 input ready;
12. no float/double canonical authority;
13. no VM81 mutation, Hash72 commit, persistence mutation, or canonical proof authority;
14. preserve the I121.8 all-distinct identity census through VMIR;
15. exact and synthetic workflow lanes terminal green.

## Environment

The current execution container cannot resolve GitHub DNS, so repository edits and validation are performed through the connected GitHub repository and GitHub Actions. No local scratch state is required for continuation.

## Next action

Implement the additive I121.10 provenance producer, test, amendment, and exact/synthetic workflow. Repair only I121.10 branch-local defects. Do not modify frozen Pass159, Pass169, I121.9 semantic files, root `Makefile`, or canonical `main` to make the new workflow pass.
