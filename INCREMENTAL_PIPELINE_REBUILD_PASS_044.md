# Pass 044 — Incremental Pipeline Rebuild

- Builds rebuild plans from semantic dependency index hits.
- Rebuilds affected surfaces only.
- Writes rebuilt plans into the semantic composition cache as compact residue records.
