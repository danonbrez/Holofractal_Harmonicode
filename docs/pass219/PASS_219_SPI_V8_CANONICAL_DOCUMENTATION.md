# Pass 219 SPI v8 — Canonical Documentation Map

**Status:** CURRENT REFERENCE / ADDITIVE DOCUMENTATION  
**Canonical merged base:** `main @ 18f6a1899d4009bdeeeaf95d536dfe2857198458`  
**Merged lineage:** PR #427 — scalar projection through computational determinism v8

This document is the current navigation and interpretation layer for the Pass 219 Scalar Projection Interface (SPI) lineage now merged to `main`. It does not replace or rewrite historical pass artifacts. Earlier contracts, white papers, restart records, and validation receipts remain immutable evidence of the state in which they were created.

Repository code and versioned contracts remain authoritative over explanatory prose.

## 1. Current SPI lineage

The merged SPI stack is additive:

```text
scalar projection proof layer
-> matrix/tensor scalar projection
-> Law of 1 normalization
-> equal-sum / Fibonacci / cubic tensor translation
-> constraint-evolution learning optimizer
-> octonion reciprocal/base-pair dimensional lift
-> computational determinism invariant
```

The corresponding normative contracts are:

```text
contracts/pass219/PASS_219_SPI_SCALAR_PROJECTION_PROOF_LAYER_V2.md
contracts/pass219/PASS_219_SPI_MATRIX_TENSOR_SCALAR_PROJECTION_RULES_V1.md
contracts/pass219/PASS_219_SPI_LAW_OF_ONE_SCALAR_NORMALIZATION_V1.md
contracts/pass219/PASS_219_SPI_EQUAL_SUM_FIBONACCI_CUBIC_TENSOR_TRANSLATION_V1.md
contracts/pass219/PASS_219_SPI_CONSTRAINT_EVOLUTION_LEARNING_OPTIMIZER_V1.md
contracts/pass219/PASS_219_SPI_OCTONION_RECIPROCAL_BASEPAIR_DIMENSIONAL_LIFT_V1.md
contracts/pass219/PASS_219_SPI_COMPUTATIONAL_DETERMINISM_INVARIANT_V1.md
```

## 2. Authority order

The SPI lineage does not create a second canonical state machine.

```text
source / explicit instruction
-> exact typed proposal
-> SPI projection / proof / candidate construction
-> invariant verification
-> deterministic candidate decision
-> singleton VM81 canonical admission path
-> Hash72 receipt lineage
-> Hash216 ordered transition identity / indexing
```

Binding rules:

1. VM81 remains the singleton canonical mutation/admission authority.
2. Hash72 remains on the inherited canonical receipt path.
3. Hash216 remains on the inherited ordered transition-memory/index path.
4. SPI projection equality does not imply native identity.
5. Learning/optimization may rank or reuse candidates but cannot independently commit canonical state.
6. Semantic descriptions may construct candidates but have no direct execution authority.
7. Computational determinism is an explicit enforced invariant of verified bounded SPI execution.

## 3. Native equality versus scalar projection

For native HARMONICODE expressions `A` and `B`, the following are distinct claims:

```text
A ==_H B
pi(A) == pi(B)
```

The second does not imply the first unless an applicable registered projection proves an injective reverse relation over the active domain.

The current scalar normalization layer includes the source-bound Law of 1 surface:

```text
1=a²,x⁴,y⁴,z⁴,w⁴,∆,P²-pq,t³-t,m²-m,e^x²O,c²-b²,b²/2u⁷²
```

and the local/global bridge:

```text
pi_L(a²)=1
pi(∆)=1
```

These are projection statements. They do not erase ordered native phase identity.

## 4. Tensor translation stack

The current exact tensor translation layer includes:

```text
EQUAL_SUM_A2_NORMALIZATION
-> FIBONACCI_PYTHAGOREAN_SCALE
-> TENSOR_PAIR_CUBIC_THREE_SET
```

Equal-sum translation uses exact equation families rather than cellwise identity. The validated examples are:

- Lo Shu 3×3: eight row/column/principal-diagonal equations at sum 15;
- Sudoku 9×9: 27 row/column/bank equations at sum 45.

The exact square-state recurrence is:

```text
a²=1
b²=2
c²=a²+b²=3
Q[n+1]=Q[n]+Q[n-1]
lambda[n]=Q[n+1]/Q[n]
```

Finite ratios remain exact. The Golden limit remains symbolic under its own proof object.

The tensor-pair three-set preserves native `t` while exposing the projected residual:

```text
t³=t+a²
t³-t=a²=∆=1
pi(t³-t-a²)=0
```

## 5. Constraint-evolution learning optimizer

The v6 lifecycle is executable:

```text
FORMALIZE
-> PROVE
-> IMPLEMENT
-> OPTIMIZE
-> CANONIZE
-> ITERATE
```

Only candidates satisfying the formal/proof/implementation gates enter optimization.

The reference structural objective is exact:

```text
unresolved = branch_count - reusable_branch_count
```

with deterministic ranking:

```text
minimum unresolved
-> maximum already-proved reuse
-> stable candidate ID
```

The optimizer binds candidates to exact 5,184 coordinates:

```text
address5184 = cell81 * 64 + operation64
```

and carries the four-lane hydration coordinate as typed metadata. These coordinates do not grant canonical mutation authority.

## 6. Octonion reciprocal/base-pair dimensional lift

The v7 source syntax is preserved verbatim:

```text
x=1/y y=-x
(x,y,z,w)²==(Ixy, I-yx, Izw, I-wz)²
```

Three relation families remain distinct:

```text
RML2 geometric phase opposite:
  x <-> z
  y <-> w

RML4 ordered reciprocal operand:
  x <-> y
  z <-> w

symbolic base-pair equivalent:
  x -> Ixy
  y -> I-yx
  z -> Izw
  w -> I-wz
```

The first-principles relational lift is:

```text
D1 = s
D2 = (s, R_phase(s))
D3 = (s, R_phase(s), B(s))
D4 = (s, R_phase(s), B(s), B(R_phase(s)))
```

For `D[n>4]`, the implementation records recursive closure over the same algebra plus exact ancestry. It does not introduce a new octonion basis or materialize an exponential table.

### Typed imaginary-phase collapse

The v7 gyroscope may be encoded into the inherited ordinary `u^72` imaginary-rotation coordinate system without information loss **only through the complete typed carrier**. The carrier retains source channel, `phase72`, plane/orientation, geometric opposite, reciprocal operand, symbolic base pair, opposite base pair, and source-relation provenance.

The theorem is:

```text
Restore(Collapse(G)) = G
```

for the represented primitive channel/phase domain.

A bare `phase72` value is not claimed to contain the full native ordered state.

## 7. Compatibility with `a²=1`

The scalar unit projection and the richer phase geometry coexist because they are different typed surfaces:

```text
pi_L(a²)=1
```

does not imply:

```text
xy == yx
x == z
y == w
native phase state == a²
```

This is the central mechanism by which scalar normalization remains exact while ordered noncommutative phase ancestry remains available to the runtime.

## 8. Computational determinism invariant

A verified bounded task envelope is:

\[
J=(I,\Sigma,\Omega,B,\mathcal I)
\]

where:

- `I` is the explicit instruction;
- `Σ` is the explicit authorized scope;
- `Ω` is the typed closing condition;
- `B` is the finite exact-integer step bound;
- `𝓘` contains `I_DET_COMPUTATIONAL_DETERMINISM`.

Only after task formation succeeds does the operational outcome theorem apply:

\[
D(J,s,C)\in\{ADVANCE,HALT\}.
\]

The current HALT reason classes are:

```text
CLOSED
REJECTED
QUARANTINED
NULL_BRANCH
RESOURCE_BOUNDED
STABLE_UNRESOLVED
```

These are classifications beneath `HALT`, not independent actions.

The hardened selection rule is:

```text
MIN_TRANSITION_ORDINAL
-> STABLE_CANDIDATE_ID
```

Candidate receipt bytes verify integrity but do not break ties or select transitions. Semantic labels have no selection authority. Duplicate stable IDs produce `HALT(QUARANTINED)`.

## 9. Deterministic replay

For identical canonical task, state, and candidate inputs, the execution relation must reproduce the same decision structure and receipt.

A replay mismatch is therefore an invariant failure, not a permissible alternate execution history.

The reference v8 test surface verifies:

- candidate enumeration order independence;
- semantic-label independence;
- duplicate-ID quarantine;
- receipt tamper detection;
- explicit instruction/scope/closure/bound requirements;
- scope and invariant closure;
- all bounded HALT reason classes;
- float rejection;
- exact deterministic replay.

## 10. Frozen validation evidence

The merged v8 lineage was validated before merge.

### v8

```text
workflow: Pass 219 SPI Computational Determinism v8
run:      34619424085
job:      103329393361
semantic head: c2f4aff3482027800517b19dec5151d25779a146
```

Validation included:

```text
15 computational determinism tests passed
6 registry v8 tests passed
12 frozen v7 dimensional-lift tests passed
7 frozen registry v7 tests passed
23 inherited RML2/RML4 geometry tests passed
```

Evidence hashes:

```text
task receipt:        8ad5096d90aa0fabe13ffbc3f1963270eda470f2fc9e18d5bc687fc43ea822e4
ADVANCE receipt:     70fdc369b945ab2511b5e923acefb7d5a768b38dc2b0c2805c49d573c488b20d
CLOSED HALT receipt: 0c3b922cc50aa24ffb97aae27befd313b0ce58f4cd68aed88ddf042a2275a5e4
determinism witness: b6f1fd64796a5cf35daa8ac8a67bf3e12497b991f955dfe1a8a7cdd5ce161cb5
registry v8:         be0576041742f0ba37187bd032f736e69d7d44bedb73ee934bd939e3e66ac6d0
```

Artifact:

```text
10271721780  pass219-spi-computational-determinism-v8
sha256:8b0306760919aabed89a3f8c9d9e2ce8b42d6bb390821005da776452fc35708c
```

### Frozen corpus

```text
PROVEN:              429
SYMBOLIC:             43
MISSING_PROJECTION:    0
UNSUPPORTED_DOMAIN:    0
manifest: 481c0bb0264ad0771344ae068624dcfd7c9c5ba853a8c7963ad9a22971389aee
classification_complete: true
scalar_value_complete: false
```

`scalar_value_complete=false` remains intentional. Unresolved source surfaces remain unresolved rather than being silently scalarized.

## 11. Current executable files

```text
hhs_spi_octonion_dimensional_lift_v1.py
hhs_spi_octonion_dimensional_lift_tests_v1.py
hhs_spi_computational_determinism_invariant_v1.py
hhs_spi_computational_determinism_invariant_v2.py
hhs_spi_computational_determinism_invariant_tests_v2.py
hhs_spi_scalar_projection_registry_v7.py
hhs_spi_scalar_projection_registry_tests_v7.py
hhs_spi_scalar_projection_registry_v8.py
hhs_spi_scalar_projection_registry_tests_v8.py
```

Dedicated workflows:

```text
.github/workflows/pass219-spi-octonion-dimensional-lift-v7.yml
.github/workflows/pass219-spi-computational-determinism-v8.yml
```

## 12. Tutorials and papers

Current companion documentation:

```text
docs/tutorials/HARMONICODE_SPI_V8_TUTORIAL.md
docs/whitepapers/HARMONICODE_OCTONION_RECIPROCAL_BASEPAIR_DIMENSIONAL_LIFT_THEOREM.md
docs/whitepapers/HARMONICODE_COMPUTATIONAL_DETERMINISM_AND_BOUNDED_EXECUTION_THEOREM.md
docs/HARMONICODE_SPI_V8_REGISTRY_ADDENDUM.md
```

Historical explanatory papers remain useful for the layer they document, but their revision date must not be read as overriding this merged SPI lineage.

## 13. Open proof obligations

The merged documentation does not convert open proof obligations into established results.

In particular, O3 remains fail-closed: the exact constant literal is verified, but the specific provenance from `f == t/m` is not yet closed.

The general rule remains:

```text
UNRESOLVED != FALSE
UNPROVED != DISPROVED
IMPLEMENTED ON ONE SUBSTRATE != RESTRICTED TO THAT SUBSTRATE
```

Formal status is determined by the applicable contract, proof object, executable validation, and receipt evidence.
