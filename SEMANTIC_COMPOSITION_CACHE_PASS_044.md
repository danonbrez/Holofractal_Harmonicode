# Pass 044 — Semantic Composition Cache

- Stores kernel-derived composition entries with exact verbatim semantic text.
- Uses guarded semantic memory writes/searches through `hhs_semantic_memory_guard_v1`.
- Uses Lo Shu phase embedding receipts as advisory ML/search geometry.
- Uses receipt-vector nearest search as advisory retrieval, not runtime authority.
- Cache entries retain compact roots and reconstruction recipes; expanded pipeline payloads are not persisted.
