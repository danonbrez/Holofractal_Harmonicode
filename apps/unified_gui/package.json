{
  "name": "hhs-pass157-unified-gui",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "vite build",
    "verify": "npm test && npm run build"
  },
  "dependencies": {
    "three": "0.170.0"
  },
  "devDependencies": {
    "vite": "5.4.14"
  }
}
