# Workspace Object Schema Registry — Pass 049

Every workspace object uses `HHS_RUNTIME_WORKSPACE_OBJECT_V1` and resolves to `HHS_WORKSPACE_OBJECT_REFERENCE_V1`. Objects carry project identity, object type, modality, source roots, current roots, lifecycle state, receipt tips, and reconstruction recipes.
