# Next Pass 046 — GUI Live Runtime Console Binding

Recommended next step:

```text
connect GUI runtime panels directly to the four FastAPI websocket channels
render live kernel ticks, receipts, graph projections, and transport state
add browser-facing smoke tests for live socket ingestion
```

Pass 046 should verify that the GUI no longer displays demo/runtime-placeholder data and that live kernel event fields are visible in the runtime console.
