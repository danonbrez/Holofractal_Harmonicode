# Pass 202 Restart Record

## Repository state

- Repository: `danonbrez/Holofractal_Harmonicode`
- Branch: `agent/pass202-high-fidelity-native-render`
- Merge target: `main`
- Base commit: `bdf19276b0974481bd69d70ca1154f284f238e48`
- Contract: `HHS-P202-HF-NATIVE-RENDER-PARAMETER-AUTHORITY-VM81-H72-H216`
- Classification target: `HHS_PASS_202_HIGH_FIDELITY_NATIVE_RENDER_AUTHORITY_VERIFIED`

## Authorized scope

- inherit the complete system through Pass 201;
- repair storybook native build-path discovery for sibling or vendored game sources;
- expose the complete native style, reciprocal palette, texture-layer, sprite-overlay, timing, presentation, codec, and audio parameter surface;
- make texture and sprite layer selection effective through the native storybook projection bridge;
- replace the fixed nearest-neighbor and black-pad production transport with configurable high-fidelity portrait composition;
- preserve exact native frame, Hash72, Hash216, replay, opcode, and receipt authority;
- expose high-fidelity presets, deterministic resolution, contextual candidate scoring, API routes, OpenAPI, and visual controls.

## Initial observed defects

- `native_projects/hhs_storybook_reel/Makefile` assumes `../hhs_vm81_game_level10` even when game sources are vendored under the storybook project;
- production encoding is hard-coded to `scale=1080:972:flags=neighbor,pad=1080:1920:0:474:color=0x0b0910`;
- the API accepts only template and Style V2 overrides;
- texture layers and sprite overlays are not selectable by storybook requests;
- contextual defaults return one opaque template rather than scored alternatives;
- no complete typed parameter catalog exists.

## Validation remaining

- native projection bridge compilation and tests;
- Python parameter authority and filter-graph tests;
- API and OpenAPI tests;
- visual studio parameter-control tests;
- production diagnostic render and manifest validation;
- canonical evidence binding;
- removal of unrelated workflow-generated commits;
- PR readiness, merge, and main verification.

## Next action

Implement the native projection bridge and high-fidelity runtime, then run dependency-scoped tests before production render validation.
