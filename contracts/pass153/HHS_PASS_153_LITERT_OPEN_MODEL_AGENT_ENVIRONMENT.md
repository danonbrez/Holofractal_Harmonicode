# HHS PASS 153 — LiteRT Open-Model Agent Environment

## Normative identity

- Contract ID: `HHS-P153-LITERT-OPEN-MODEL-AGENT`
- Pass number: `153`
- Inheritance parent: complete authoritative legacy nucleus through Pass 152 and all merged pre-ratification additions
- Model role: replaceable advisory interface, speculative decoder, and API/CLI command translator
- Semantic execution and state-commit authority: VM81
- Runtime receipt authority: Hash72
- Independent identity and index authority: Hash216
- Terminal classification: `HHS_PASS_153_LITERT_OPEN_MODEL_AGENT_ENVIRONMENT_VERIFIED`

## Required product

Pass 153 provides a plug-and-play local open-model environment with:

1. a bundled deterministic open reference language model for offline boot and validation;
2. a manifest-driven LiteRT interpreter adapter for replaceable `.tflite` models;
3. governed model, session, and tool registries;
4. callable API, CLI, Python SDK, model, tool, and runtime surfaces;
5. a GUI exposing dashboard, model manager, agent workspace, session manager, tool registry, receipt/replay, and deployment status through governed APIs only;
6. a reproducible Docker server package;
7. a complete Hugging Face Docker Space package;
8. deterministic replay and Hash72 receipt closure;
9. one canonical release commitment binding the GitHub source tree, full inherited ZIP, and Hugging Face package.

## Authority law

No model output, GUI action, CLI command, SDK request, or tool result becomes authoritative merely because it was generated or returned. Model output remains advisory until validated. Every mutation remains subject to inherited capability policy, VM81 admission, state transition rules, and Hash72 receipt closure. The model has no direct access to VM81 memory, internal databases, secrets, unrestricted shell execution, or undeclared tools.
