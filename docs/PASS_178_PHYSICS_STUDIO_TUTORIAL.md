# Pass 178 Exact Physics Studio — I148 nucleus

## Current classification

`P178_EXACT_PHYSICS_NUCLEUS_NONTERMINAL`

I148 converts the previously contract-only Pass 178 into an executable exact-physics nucleus. It does not claim the terminal relativistic/quantum simulation runtime.

## Authority direction

```text
byte-preserved model source
  -> exact model registration
  -> exact candidate state
  -> domain / invariant validation
  -> inherited VM81 admission
  -> post-VM81 Hash72 execution evidence
  -> archival Hash216 transition/state identity
  -> immutable Q32.32 render packet
  -> browser projection
```

Renderer, GPU, browser and capture code have no simulation mutation authority.

## Implemented nucleus

- exact integers/rationals and exact complex rationals;
- symbolic algebraic-root branch witnesses;
- typed constraint graph;
- contract-visible Harmonicode membrane;
- exact relativistic proper-time free step;
- mass-shell algebraic positive-energy witness;
- exact finite Hermitian quantum Cayley step;
- byte-preserving source ingress;
- immutable model registration;
- candidate validation without clock advancement;
- inherited VM81 initial/step admission;
- deterministic replay chain;
- immutable render packets with rational Q32.32 error witness;
- C public ABI nucleus;
- Relativity, Quantum and Harmonicode templates;
- served `/physics-studio/` and governed HTTP API.

## Scientific labeling

The UI and runtime keep standard equations separate from HHS admissibility constraints. The I148 corpus is explicitly the contract-visible corpus nucleus and is not represented as the complete historical user-supplied corpus required for terminal Pass 178 closure.

## Remaining terminal work

The complete source corpus, CST/AST/HIR compiler, full thermodynamic kernel, charged-particle field laboratory, full double-slit laboratory, registered measurement authority, singular Hash72 commit integration, executing Three.js viewport, deterministic MP4 capture, browser/mobile E2E, performance/security evidence, and authoritative-main integration remain repair-forward work.
