# ============================================================
# Holofractal Harmonicode (HHS)
# Runtime / Backend / Graph / Audit Environment
# ============================================================

# ------------------------------------------------------------
# Core backend runtime
# ------------------------------------------------------------

fastapi>=0.111,<1.0
pydantic>=2.7,<3.0
uvicorn[standard]>=0.30,<1.0

# ------------------------------------------------------------
# HTTP / API utilities
# ------------------------------------------------------------

httpx>=0.27,<1.0
requests>=2.32,<3.0

# ------------------------------------------------------------
# Websocket / async runtime
# ------------------------------------------------------------

websockets>=12.0,<16.0
anyio>=4.4,<5.0

# ------------------------------------------------------------
# Runtime graph / persistence helpers
# ------------------------------------------------------------

networkx>=3.3,<4.0

# ------------------------------------------------------------
# Serialization / schema tools
# ------------------------------------------------------------

orjson>=3.10,<4.0
pyyaml>=6.0,<7.0

# ------------------------------------------------------------
# Numerical / symbolic runtime support
# ------------------------------------------------------------

numpy>=1.26,<3.0
sympy>=1.13,<2.0

# ------------------------------------------------------------
# Runtime storage
# ------------------------------------------------------------

sqlalchemy>=2.0,<3.0

# Optional lightweight local DB helpers
aiosqlite>=0.20,<1.0

# ------------------------------------------------------------
# CLI / runtime utilities
# ------------------------------------------------------------

rich>=13.7,<14.0
click>=8.1,<9.0
typer>=0.12,<1.0

# ------------------------------------------------------------
# Logging / diagnostics
# ------------------------------------------------------------

loguru>=0.7,<1.0

# ------------------------------------------------------------
# Testing / certification / regression
# ------------------------------------------------------------

pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0

# ------------------------------------------------------------
# Development / formatting
# ------------------------------------------------------------

black>=24.4,<25.0
isort>=5.13,<6.0

# ------------------------------------------------------------
# Optional runtime validation / typing
# ------------------------------------------------------------

typing-extensions>=4.12,<5.0

# ============================================================
# Notes
# ============================================================
#
# Primary backend bootstrap:
#   python -m hhs_backend.server
#
# Core validation:
#   python hhs_runtime_smoke_tests_v1.py
#   python hhs_regression_suite_v1.py
#   python hhs_v1_bundle_runner-2.py
#
# Repository currently contains:
#   - compatibility shims
#   - canonical package runtime layers
#   - backend orchestration
#   - websocket runtime streaming
#   - graph/state persistence
#   - ctypes/C runtime bridges
#   - multimodal runtime tooling
#
# ============================================================