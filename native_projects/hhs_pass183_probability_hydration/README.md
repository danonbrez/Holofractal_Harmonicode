# Pass 183 native probability hydration ABI

This nucleus exposes every required `hhs_p183_*` ABI symbol through a bounded C11 context. It preserves the canonical formula and ordered reciprocal lane tokens, validates balanced parenthetical membranes and `n MOD (n+1)` boundary identities, exposes typed zero-bypass, applies the global modulus only after closure, emits deterministic 72/216-character identities, and verifies replay and receipt integrity.

The high-level exact arbitrary-precision adapter implementation is in `hhs_runtime/pass183/`; the C nucleus is the native lexical, membrane, closure, residue, identity, and replay boundary used by the acceptance gate.
