# Workspace Command Authority Map — Pass 049

Command tier is declared by `HHS_WORKSPACE_COMMAND_ENVELOPE_V1`; it is never inferred from a UI control. Presentation-only commands remain local. Runtime reads, mutations, interpreter, compiler, and emulator commands require FastAPI/kernel authority.
