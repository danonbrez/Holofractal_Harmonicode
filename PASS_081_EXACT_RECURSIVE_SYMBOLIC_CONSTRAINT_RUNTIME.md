# Pass 081 — Exact Recursive Symbolic Constraint Runtime

Pass 081 is additive to Pass 080. Execution begins only from `ADMITTED_FOR_LEASED_NATIVE_INVOCATION`. The Python layer parses, orchestrates, verifies, and receipts symbolic manifold evolution; existing VM81 bindings retain native authority.

Canonical rules: no floating-point authority; `==` is a scoped bidirectional gate; each gate owns one local `P_g`; universal non-P symbols retain identity across occurrences; unresolved is not false; ordered products and token paths remain directional; Hash72 binds source, ancestry, topology, phase projection, admission, and closure state.
