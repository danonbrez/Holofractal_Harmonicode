# Pass 219 I1.28 — inherited Pass 198 repaired calibration-registry exposure

## Classification

`INHERITED_IMPLEMENTATION_REPAIR_AND_MEMBRANE_EXPOSURE`

Pass 198 remains one inherited layer of the cumulative HHS execution image. I1.28 does not replace Pass 198, create a second calibration authority, or grant Pass 219 a new mutation path. It exposes the repaired Pass 198 registry through the cumulative exact C ABI and C++ RNA namespace after the repaired implementation and immediate successors were dependency-scoped validated.

## Historical provenance

```text
primary PR: 136
historical base: b40e11315840781d1fd9c12932fad46eb32e383f
reviewed historical head: a383ab8ec6a55e04ab490477c7b8cfe5d107d098
accepted squash merge: 122d21565fd7f3f9bbe9fb73ad2182d1d468ba5e
frozen I1.27 predecessor: fa89488d84f845fa372551b5324e0ddd37e49daf
validated I1.28 repair checkpoint: 97faba2ec59c54d1cd17be5bb88ade370841f65f
```

The accepted merge remains historical provenance. The repaired current V1 surface is bound because Pass 199 and Pass 200A inherit and call that exact class directly.

## Bound repair obligations

I1.28 binds all 13 PR #136 review findings:

1. deterministic full replay before verified simplification proof;
2. proof coverage bound only to executed states with nonzero admitted coverage;
3. unsupported executable axis schemas fail closed;
4. VM81 registration receipt persisted in the registration event/document;
5. recursive floating-point rejection across identity-bearing specification values;
6. atomic built-in registration under the write transaction;
7. normalized simplification IDs used for persistent updates;
8. promotion state rechecked inside the transaction;
9. reusable checkpoint identity independent of a new tick receipt;
10. promotion requires distinct verified workload identities;
11. executable Pass197 adapter bound to the exact approved built-in specification;
12. no false aggregate per-simplification savings claim;
13. all six declared negative mutations execute and persist fail-closed evidence before `ENVELOPE_VERIFIED`.

Finding 13 evidence uses `HHS_PASS_198_EXECUTED_NEGATIVE_MUTATION_EVIDENCE_V1`. Every mutation result carries `executed`, `detected`, an exact outcome/details payload, a Hash72 evidence identity, and a shared evidence-root Hash72.

## Exact production boundary

The bound production envelope is:

```text
parameter states: 405
admitted states: 320
domain rejected states: 85
VM5184 address comparisons: 1,658,880
verified simplifications: 4
executed negative mutations: 6
review findings: 13
```

Canonical arithmetic remains exact. The membrane does not authorize floating-point canonical computation.

## C ABI / C++ RNA exposure

C ABI:

```text
HHSExactPass198RepairedCalibrationRegistryWitnessV1
HHSExactPass219InheritedPass198BindingV1
hhs_exact_pass219_inherited_pass198_version
hhs_exact_pass219_bind_pass198_repaired_calibration_registry
```

C++ RNA surface:

```text
hhs::rna::InheritedPass198RepairedCalibrationRegistry
```

The C++ layer is a validator/binding surface only. It has no mutation, persistence, Hash72-clock, candidate, API, or VM81 mutation authority.

## Successor preservation

The membrane explicitly reuses the frozen I1.27 Pass199 source evidence and validates that the accepted/repaired Pass199 identity remains unchanged. Pass199 remains the immediate successor, and its inherited singleton calibration commit path remains intact.

## Pre-membrane hosted evidence

All required gates were terminal green on repair checkpoint `97faba2e...`:

```text
Pass198 I128 thirteen-finding validation: 32770921677
Pass198 production/integration:            32770921723
Pass199 production:                        32770921637
Pass200A production:                       32770921758
Pass200B production:                       32770921660
Frozen I127 exact/synthetic preservation:  32770921681
VM81 Exact ABI:                            32770921615
UQCEL:                                     32770921651
```

## Authority boundary

I1.28 adds no new canonical writer. VM81/kernel authorization remains inherited. Hash72 remains evidence/receipt identity and Hash216 remains archival proof after valid closure. The registry, API, worker, C ABI, C++ wrapper, cache, and proof storage do not independently grant mutation authority.

## Freeze rule

I1.28 is not frozen merely because the membrane sources exist. The membrane must pass exact and synthetic validation, after which the documentation-inclusive seal commit is validated again. Only that final validated commit may be labeled frozen.
