# HARMONICODE Genesis / Reverse-Offset Holographic Nucleus Theorem

Let the Lo Shu magnitude coordinate be `m in {1,...,9}`.

Define the Genesis offset:

~~~text
epsilon = m - 5
~~~

and the reverse offset:

~~~text
r = 5 + 2*epsilon = 2*m - 5.
~~~

Then the three coordinate maps are exact and mutually invertible on their canonical alphabets:

~~~text
m = epsilon + 5
epsilon = (r-5)/2
m = (r+5)/2.
~~~

Applied cellwise to the Lo Shu nucleus, they produce three co-resident representations of the same nine addresses.

The Genesis layer is zero-centered:

~~~text
row sums = column sums = diagonal sums = 0.
~~~

The magnitude and reverse-offset layers retain:

~~~text
row sums = column sums = diagonal sums = 15.
~~~

The center remains the same cell under all three views:

~~~text
0 <-> 5 <-> 5.
~~~

Reflection around the nucleus center is retained:

~~~text
m + (10-m) = 10
epsilon + (-epsilon) = 0
r + (10-r) = 10.
~~~

Therefore the three layers are not independent data sets. They are exact, redundant coordinate realizations of one constrained Lo Shu nucleus.

Repeated across nine nuclei:

~~~text
9*9 = 81
81*64 = 72^2 = 5184.
~~~

I036 is constructor-local only and makes no canonical-service promotion.
